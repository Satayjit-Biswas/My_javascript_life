var a,b,c,d,f;
a=10;
    // its assignment operator
b=20;
d=10;
c=a+b;
    // its Arithmetic operator +,_,*,/,%,++,-- etc.
d==a;
    // comparison operator
        // == equal operator
        // === equal and same type operator
        // != not equal operator
        // !== NOT equal and not same type operator
        // >= greater then and equal 
        // <= less then and equal
if(a==d && a==c) {
    console.log(d)
}
    //Logical operator
        // && and operator
        // || or operator
        // ! not operator
    
let x = (b < 18) ? "y":"z";
    //conditional operator && Ternary Operator

//comparison operation
// > greater then
// < less then
// == equal
// >= equal and greater then
// <= equal and less then
console.log(x)